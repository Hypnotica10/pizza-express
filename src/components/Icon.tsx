import React from "react";

type Props = {
  name: string;
};

const Icon = ({ name }: Props) => {
  switch (name) {
    case "": {
      return <></>;
    }
    default: {
      return <></>;
    }
  }
};

export default Icon;
